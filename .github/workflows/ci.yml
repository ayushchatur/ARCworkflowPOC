name: CI Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v1
    - name: Runa one-line sctips
      run: echo "hello world!"
    - name: Mutli line
      run: |
        echo "Ayush \n"
        for dir in */;do ls -la $dir | head -10 ; echo "~~~~~~";done
        